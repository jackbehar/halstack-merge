import { DxcToggleGroup } from "@dxc-technology/halstack-react";
import Props from "@dxc-technology/halstack-react/toggle-group/types";

export default function ToggleGroup(props: Props) {
  return <DxcToggleGroup {...props} />;
}
